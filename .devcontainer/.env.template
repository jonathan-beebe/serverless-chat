# Copy to .devcontainer/.env and fill in your values, or run scripts/setup-devcontainer.sh.
# Loaded into the devcontainer at start; used to configure git inside the container.
GIT_USER_NAME=
GIT_USER_EMAIL=
